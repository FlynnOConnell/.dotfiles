- defaults:
    link:
      relink: true
      create: true
      force: true

- clean: ['~']
- clean:
    ~/:
    ~/.config:
      recursive: true

- link:
    ~/.local/bin/tmux-sessionizer:
      path: dots/.local/bin/tmux-sessionizer
    ~/.bashrc:
      path: dots/.bashrc
    ~/.ideavimrc:
      path: dots/.ideavimrc
    ~/.zshrc:
      path: dots/.zshrc
    ~/.config/vim:
      path: dots/.config/vim
    ~/.config/spicetify:
      path: dots/.config/spicetify
    ~/.config/BetterDiscord:
      path: dots/.config/BetterDiscord
    ~/.config/github-copilot:
      path: dots/.config/github-copilot
    ~/.config/lazygit:
      path: dots/.config/lazygit
    ~/.config/neofetch:
      path: dots/.config/neofetch
    ~/.config/kitty:
      path: dots/.config/kitty

    # Non-Windows Configs ---
    ~/.config/nvim:
      path: dots/.config/nvim
    ~/.config/tmux:
      path: dots/.config/tmux

    # macOS configs ---
    ~/.config/yabai:
      path: dots/.config/yabai
      if: '[ `uname` = Darwin ]'
    ~/.config/sketchybar:
      path: dots/.config/sketchybar
      if: '[ `uname` = Darwin ]'
    ~/.config/skhd:
      path: dots/.config/skhd
      if: '[ `uname` = Darwin ]'

    # Windows Configs ---
    ~/.config/komorebi:
      path: dots/.config/komorebi
    ~/.config/wkhdrc:
      path: dots/.config/whkd/whkdrc
    ~/AppData/local/nvim:
      path: dots/.config/nvim

    # Linux configs ---
    ~/.Xmodmap:
      path: .Xmodmap
      if: '[ `uname` = Linux ]'
    ~/.fonts:
      path: .fonts
      if: '[ `uname` = Linux ] '
    ~/.icons:
      path: .icons
      if: '[ `uname` = Linux ] '
    ~/.xsettingsd:
      path: .xsettingsd
      if: '[ `uname` = Linux ] '
    ~/.config/gtk-2.0:
      path: .config/gtk-2.0
      if: '[ `uname` = Linux ]'
    ~/.config/gtk-3.0:
      path: .config/gtk-3.0
      if: '[ `uname` = Linux ]'
    ~/.config/i3:
      path: .config/i3
      if: '[ `uname` = Linux ]'
    ~/.config/bottom:
      path: .config/bottom
      if: '[ `uname` = Linux ]'
    ~/.config/polybar:
      path: .config/polybar
      if: '[ `uname` = Linux ]'
    ~/.config/rofi:
      path: .config/rofi
      if: '[ `uname` = Linux ]'
    ~/.config/flameshot:
      path: .config/flameshot
      if: '[ `uname` = Linux ]'
    ~/.config/dunst:
      path: .config/dunst
      if: '[ `uname` = Linux ]'
    ~/.config/picom:
      path: .config/picom
      if: '[ `uname` = Linux ]'

