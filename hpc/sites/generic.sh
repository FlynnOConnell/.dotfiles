#!/usr/bin/env bash
# site: generic - an unrecognised cluster or plain server.
# everything falls back to $HOME (see hpc/env.sh). set HPC_USER / HPC_DATA in
# ~/.bashrc.local if this box has real scratch space worth pointing at.
